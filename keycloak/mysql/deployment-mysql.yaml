apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
spec:
  selector:
    matchLabels:
      app: mysql
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - image: mysql:5.6
        name: mysql
        envFrom:
          - configMapRef:
              name: mysql-env
        ports:
        - containerPort: 3306
          name: mysql-service
        resources:
          requests: 
            cpu: "0.02"
            memory: 20Mi
          limits:
            cpu: "0.02"
            memory: 20Mi          
        volumeMounts:
        - name: mysql-persistent-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-persistent-storage
        persistentVolumeClaim:
          claimName: mysql-pv-claim